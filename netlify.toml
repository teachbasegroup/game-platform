# Skips a Netlify build unless index.html or this file changed,
# so doc-only commits don't burn build credits.

[build]
  publish = "."
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- index.html netlify.toml"
